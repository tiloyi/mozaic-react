---
to: "<%= hasDocs ? `src/components/${name}/${name}.stories.mdx` : null %>"
---
import {
  Meta,
  Story,
  Canvas,
  Source,
  ArgsTable,
} from '@storybook/addon-docs';
import { <%= name %>Story, argTypes } from './<%= name %>.stories';
import <%= name %> from './<%= name %>';

<Meta title="<%= name %>" component={<%= name %>} argTypes={argTypes} />

# <%= name %>

The `<%= name %>` component is the **React.js** implementation of the **Mozaic <%= name %>** component.

You can find all the characteristics of this component by going to [the associated documentation](https://mozaic.adeo.cloud/Components/<%= name %>/).

## Usage

To use the component in your project you must:

1. Import the component where you need it:

<Source
  language="javascript"
  code="import { <%= name %> } from '@mozaic-ds/react';"
/>

2. Call the component and give it the desired props, for example:

<Canvas withSource="open">
  <Story story={<%= name %>Story} />
</Canvas>


## Props

<ArgsTable of={<%= name %>} />
