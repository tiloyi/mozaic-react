import {
  Meta,
  Story,
  Canvas,
  Source,
  ArgsTable,
} from '@storybook/addon-docs';
import { Text, WithLeftIcon, WithRightIcon, IconOnly, argTypes } from './Button.stories';
import Button from './Button';

<Meta title="Components/Button" component={Button} argTypes={argTypes} />

# Button

The `Button` component is the **React.js** implementation of the **Mozaic Button** component.

You can find all the characteristics of this component by going to [the associated documentation](https://mozaic.adeo.cloud/Components/Buttons/).

## Usage

To use the component in your project you must:

1. Import the component where you need it:

<Source
  language="javascript"
  code="import { Button, IconButton } from 'bigfish-wms--mozaic-react';"
/>

2. Call the component and give it the desired props, for example:

button with text:

<Canvas withSource="open">
  <Story story={Text} />
</Canvas>

button with text and left icon:

<Canvas withSource="open">
  <Story story={WithLeftIcon} />
</Canvas>

button with text and right icon:

<Canvas withSource="open">
  <Story story={WithRightIcon} />
</Canvas>

button with icon:

<Canvas withSource="open">
  <Story story={IconOnly} />
</Canvas>

## Props

<ArgsTable of={Button} />
