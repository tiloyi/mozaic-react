import { Meta, Source, ArgsTable } from '@storybook/addon-docs';
import { ModalsProvider } from './ModalsContext';

<Meta title="Components/ModalsProvider" component={ModalsProvider} />

# ModalsProvider

Компонент представляет собой контейнер для компонентов `Modal` и `Layer`.

## Usage

To use the component in your project you must:

<Source language="javascript" code="import { ModalProvider } from '@mozaic-ds/react';" />
