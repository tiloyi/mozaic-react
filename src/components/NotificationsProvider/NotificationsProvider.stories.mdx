import { Meta, Story, Canvas, Source, ArgsTable } from '@storybook/addon-docs';
import { BottomNotifications, TopNotifications } from './NotificationsProvider.stories';
import { NotificationsProvider } from './NotificationsContext';

<Meta title="Components/NotificationsProvider" component={NotificationsProvider} />

# NotificationProvider

Компонент представляет собой контейнер для всех нотификаций.

## Usage

To use the component in your project you must:

1. Import the component where you need it:

<Source language="javascript" code="import { NotificationsProvider } from '@mozaic-ds/react';" />

2. Call the component and give it the desired props.

Bottom notifications:

<Canvas withSource="open">
    <Story story={BottomNotifications} />
</Canvas>

Top notifications:

<Canvas withSource="open">
    <Story story={TopNotifications} />
</Canvas>
