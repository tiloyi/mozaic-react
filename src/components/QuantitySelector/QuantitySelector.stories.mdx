import { Meta, Story, Canvas, Source, ArgsTable } from '@storybook/addon-docs';
import { Controls, Placeholder, Example, Values, argTypes } from './QuantitySelector.stories';
import QuantitySelector from './QuantitySelector';

<Meta title="Components/QuantitySelector" component={QuantitySelector} argTypes={argTypes} />

# QuantitySelector

The `QuantitySelector` component is the **React.js** implementation of the **Mozaic QuantitySelector** component.

You can find all the characteristics of this component by going to [the associated documentation](https://mozaic.adeo.cloud/Components/Form/QuantitySelector/).

## Usage

To use the component in your project you must:

1. Import the component where you need it:

<Source language="javascript" code="import { QuantitySelector } from '@mozaic-ds/react';" />

2. Call the component and give it the desired props, for example:

<Canvas withSource="open">
    <Story story={Controls} />
</Canvas>

with minimum and maximum values:

<Canvas withSource="open">
    <Story story={Values} />
</Canvas>

controlled example:

<Canvas withSource="open">
    <Story story={Example} />
</Canvas>

## Props

<ArgsTable of={QuantitySelector} />
