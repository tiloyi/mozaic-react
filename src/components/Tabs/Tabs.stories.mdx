import {
  Meta,
  Story,
  Canvas,
  Source,
  ArgsTable,
} from '@storybook/addon-docs';
import { TabsStory }
import Tabs from './Tabs';

<Meta title="Components/Tabs" component={Tabs} argTypes={Tabs.argTypes} />

# Tabs

The `Tabs` component is the **React.js** implementation of the **Mozaic Tabs** component.

You can find all the characteristics of this component by going to [the associated documentation](https://mozaic.adeo.cloud/Components/Tabs/).

## Usage

To use the component in your project you must:

1. Import the component where you need it:

<Source
  language="javascript"
  code="import { Tabs, TabButton } from '@mozaic-ds/react';"
/>

2. Call the component and give it the desired props, for example:

tabs button:

<Canvas withSource="open">
  <Story story={TabsStory} />
</Canvas>

## Props

<ArgsTable of={Tabs} />
