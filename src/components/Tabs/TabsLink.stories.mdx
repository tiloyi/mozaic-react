import { Meta, Story, Canvas, Source, ArgsTable } from '@storybook/addon-docs';
import { Links, argTypes } from './TabsLink.stories';
import TabsLink from './partials/Link';

<Meta title="Components/Tabs/TabsLink" component={TabsLink} argTypes={argTypes} />

# TabsLink

The `TabsLink` component is the **React.js** implementation of the **Mozaic Tabs** component.

You can find all the characteristics of this component by going to [the associated documentation](https://mozaic.adeo.cloud/Components/Tabs/).

## Usage

To use the component in your project you must:

1. Import the component where you need it:

<Source language="javascript" code="import { TabsList, TabLink } from '@mozaic-ds/react';" />

2. Call the component and give it the desired props, for example:

tabs button:

<Canvas withSource="open">
    <Story story={Links} />
</Canvas>

## Props

<ArgsTable of={TabsLink} />
