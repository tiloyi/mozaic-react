import { Meta, Story, Canvas, Source, ArgsTable } from '@storybook/addon-docs';
import { Controls } from './View.stories';
import View from './View';

<Meta title="Utilities/View" component={View} />

# View

Компонент `View` не является частью [Mozaic Design System](https://mozaic.adeo.cloud/). Предназначен для работы с внешними и внутренними отступами элемента.

## Usage

To use the component in your project you must:

1. Import the component where you need it:

<Source language="javascript" code="import { View } from 'bigfish-wms--mozaic-react';" />

2. Call the component and give it the desired props, for example:

<Canvas withSource="open">
    <Story story={Controls} />
</Canvas>

## Props

<ArgsTable of={View} />
